define(['./module'],
    function (controllers) {
      controllers.controller('ForceCollapseCtrl', ['$scope',
        function($scope) {
          $scope.title = "ForceCollapseCtrl";
        }]);

    });
