define(['./module', 'd3'],
    function (controllers) {
      controllers.controller('FocusContextAreaCtrl', ['$scope',
        function($scope) {
          $scope.title = "FocusContextAreaCtrl";
        }]);

    });
